// import { ContactPage } from '../page/contactPage';

// export class ContactAction {
//   constructor(private contact: ContactPage) {}

//   async fillForm(name: string, email: string, subject: string, message: string) {
//     await this.contact.name.fill(name);
//     await this.contact.email.fill(email);
//     await this.contact.subject.fill(subject);
//     await this.contact.message.fill(message);
//   }

//   async uploadFile(filePath: string) {
//     await this.contact.fileUpload.setInputFiles(filePath);
//   }

//   async submitForm() {
//     await this.contact.submitBtn.click();
//   }
// }